<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <style>
        /* Body styling to center the entire outline */
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #e0e0e0;
        }

        .mobile-outline {
            width: 90vw; /* Width for mobile devices */
            max-width: 350px; /* Increased maximum width for better usability */
            height: 90vh;
            border: 16px solid black;
            border-radius: 5%;
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            position: relative;
            overflow: hidden;
            display: flex;
            justify-content: center; /* Centered content */
            align-items: center; /* Centered content */
            background: url('/images/background.jpg') no-repeat center center; /* Set your background image here */
            background-size: cover; /* Ensure the background image covers the entire div */
        }

        /* Center the input container in the middle */
        .input-container {
            width: 80%;
            background: rgba(0, 0, 0, 0.6); /* Semi-transparent black background */
            padding: 20px;
            box-sizing: border-box;
            text-align: center;
            border-radius: 10px;
        }

        .motivational-line {
            font-size: 18px;
            margin-bottom: 10px;
            color: white;
            text-align: center;
        }

        input {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
        }

        .button-container {
            display: flex; /* Use flexbox to arrange buttons in a row */
            justify-content: space-between; /* Space between buttons */
            gap: 10px; /* Space between buttons */
            margin-top: 10px; /* Optional margin above buttons */
        }

        button {
            background-color: #00897b;
            color: white;
            padding: 8px; /* Decreased padding for smaller button size */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s;
            width: 48%; /* Adjusted width for smaller buttons */
        }

        button:hover {
            background-color: #00796b;
        }
    </style>
</head>
<body>

<div class="mobile-outline">
    <div class="input-container">
        <div class="motivational-line">Enter the OTP sent to your mobile number!</div>
        <input type="text" placeholder="Enter OTP" required>
        <div class="button-container">
            <button type="submit">Verify OTP</button>
            <button type="button" onclick="resendOTP()">Resend OTP</button>
        </div>
    </div>
</div>

<script>
    function resendOTP() {
        alert("OTP has been resent! Please check your mobile.");
        // Add your resend OTP logic here
    }
</script>

</body>
</html>
